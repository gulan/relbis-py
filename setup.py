#!/usr/bin/env python

from distutils.core import setup
# from setuptools import setup, find_packages

# python -m unittest discover

setup(
    name = 'relbis',
    version = '0.0.3',
    description = 'Relational DB interface in the style of Tutorial D',
    author = 'Glen Wilder',
    author_email = 'glen.wilder@gmail.com',
    packages = ['relbis'],
    scripts = [])
